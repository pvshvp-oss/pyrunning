[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
# Package dependencies for the library are already handled by setuptools
# This file is for dev dependencies for documentation generation

[dev-packages]
setuptools = "*"
build = "*"
sphinx = "*"
sphinx-rtd-theme = "*"
sphinxcontrib-mermaid = "*"
pyrunning = {editable = true, git = "https://github.com/shivanandvp/pyrunning.git"}

[requires]
python_version = ">=3.10"
